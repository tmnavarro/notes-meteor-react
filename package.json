{
  "name": "short-link-meteor-react",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha",
    "test-app": "TEST_WATCH=1 meteor test --driver-package meteortesting:mocha",
    "test-full-app": "TEST_WATCH=1 meteor test --once --full-app --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0-beta.55",
    "bcrypt": "^3.0.1",
    "meteor-node-stubs": "^0.4.1",
    "moment": "^2.22.2",
    "prop-types": "^15.6.2",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-router": "3.2.1",
    "simpl-schema": "1.5.3"
  },
  "devDependencies": {
    "chromedriver": "^2.35.0",
    "expect": "^23.6.0",
    "selenium-webdriver": "^3.6.0"
  }
}
